# URL base dell'API mock (servita dal dev server, stessa origine).
VITE_API_BASE_URL=http://localhost:5173/api
# URL base degli asset (copertine). Va combinato con il campo `coverPath` di ogni libro.
VITE_ASSET_BASE_URL=http://localhost:5173
