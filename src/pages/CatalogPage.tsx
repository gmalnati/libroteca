// TODO: elenco paginato + filtro per materia + ricerca, con lo stato nella query string.
//       Gestisci gli stati di caricamento, errore (prova ?fail=1) e risultato vuoto.
export default function CatalogPage() {
  return <h1>Catalogo</h1>;
}
